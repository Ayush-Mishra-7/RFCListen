# .dockerignore — keep the Docker image lean

.venv/
__pycache__/
.pytest_cache/
*.pyc
*.pyo
.env
tests/

# Exclude bulky cached files but keep the cache directory itself
# so the build step can write rfc_index.json into it
cache/*.txt
cache/*.xml
cache/tts/
